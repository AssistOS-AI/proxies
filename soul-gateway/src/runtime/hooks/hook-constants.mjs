export const HOOK_SCOPES = Object.freeze({ GATEWAY: 'gateway', PROVIDER: 'provider' });
export const HOOK_PHASES = Object.freeze({ REQUEST: 'request', STREAM: 'stream', RESPONSE: 'response' });
export const HOOK_TYPES = Object.freeze({ HOOK: 'hook', EXECUTOR: 'executor' });
