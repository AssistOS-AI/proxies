{
    "container": "python:3.11-slim",
    "agent": "bash /code/startup.sh",
    "cli": "bash /code/cli.sh",
    "about": "Kiro Gateway - OpenAI/Anthropic compatible proxy for Kiro API (Claude models via AWS CodeWhisperer)",
    "env": {
        "PROXY_API_KEY": {
            "required": false,
            "default": "kiro-gateway-key"
        }
    },
    "profiles": {
        "default": {
            "ports": [
                "0.0.0.0:8000:8000"
            ],
            "install": "apt-get update && apt-get install -y --no-install-recommends git curl unzip && rm -rf /var/lib/apt/lists/* && bash /code/install.sh"
        }
    }
}
